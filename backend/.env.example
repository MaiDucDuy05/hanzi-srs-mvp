# PostgreSQL connection
POSTGRES_USER=hanzi
POSTGRES_PASSWORD=change_me
POSTGRES_DB=hanzi_srs
POSTGRES_PORT=5433
DATABASE_URL=postgres://hanzi:change_me@localhost:5433/hanzi_srs

# JWT
JWT_SECRET=change_me_to_a_random_secret

# App
PORT=8000
NODE_ENV=development

# CORS - comma-separated list of allowed origins (no spaces)
ALLOWED_ORIGINS=http://localhost:5000

# Audio storage (FR-01) — MVP phục vụ file tĩnh từ thư mục cục bộ
# AUDIO_STORAGE_DIR=./storage/audio

# Admin dashboard metrics
VIP_PRICE_MONTHLY=9.99
REVENUE_TARGET_MONTHLY=45000
GEMINI_API_KEY=gemini_key