/** Gói người dùng (PR-14 entitlement). */
export enum SubscriptionPlan {
  FREE = 'FREE',
  VIP = 'VIP',
}

export enum SubscriptionStatus {
  ACTIVE = 'ACTIVE',
  EXPIRED = 'EXPIRED',
  CANCELLED = 'CANCELLED',
}
