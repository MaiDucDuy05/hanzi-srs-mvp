/** Question visibility - who can see the question */
export enum QuestionVisibility {
  PUBLIC = 'PUBLIC',
  PRIVATE = 'PRIVATE',
}

/** Question difficulty level */
export enum QuestionDifficulty {
  EASY = 'EASY',
  MEDIUM = 'MEDIUM',
  HARD = 'HARD',
}

/** Question source type - where the question comes from */
export enum QuestionSourceType {
  PRACTICE = 'PRACTICE',
  EXAM = 'EXAM',
  BOTH = 'BOTH',
}
