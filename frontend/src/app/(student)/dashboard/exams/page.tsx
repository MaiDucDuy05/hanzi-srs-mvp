import { StudentExamsFeature } from '@/features/student/student-exams-feature';

export const metadata = {
  title: 'Bài kiểm tra | Hanzi SRS',
};

export default function StudentExamsPage() {
  return <StudentExamsFeature />;
}
