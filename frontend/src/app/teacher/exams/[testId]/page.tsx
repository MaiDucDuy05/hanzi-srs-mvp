import { TestDetailFeature } from '@/features/teacher/test-detail-feature';

export const metadata = {
  title: 'Chi tiết bài kiểm tra | Hanzi SRS',
};

export default function TestDetailPage() {
  return <TestDetailFeature />;
}
