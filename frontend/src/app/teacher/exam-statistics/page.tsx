import { TeacherExamStatisticsFeature } from '@/features/teacher/teacher-exam-statistics-feature';

export const metadata = {
  title: 'Thống kê điểm số | Hanzi',
};

export default function ExamStatisticsPage() {
  return <TeacherExamStatisticsFeature />;
}
